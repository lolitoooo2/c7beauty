PORT=3000
MONGO_URI=mongodb://mongo:27017/c7beauty
JWT_SECRET=change_this_secret

# Stripe (clés test : https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# URL du frontend (liens email + redirects Stripe)
FRONTEND_URL=http://localhost:5173

# SMTP OVH
SMTP_HOST=ssl0.ovh.net
SMTP_PORT=465
SMTP_USER=contact@votre-domaine.fr
# Si le mot de passe contient $, doubler chaque $ (ex. ab$$cd pour ab$cd) — requis pour Docker Compose
SMTP_PASS=
MAIL_FROM="C7'Beauty <contact@votre-domaine.fr>"

# Désactiver le cron local (optionnel)
# DISABLE_CRON=1
